version: '3.9'

services:
  onemedical-fe:
    image: stackoverfloww/onemedical-fe:0.1.0
    env_file:
      - .env
    ports:
      - "4200:80"

  onemedical-be:
    image: stackoverfloww/onemedical-be:0.1.0
    env_file:
      - .env
    ports:
      - "8000:3000"

