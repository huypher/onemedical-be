version: '3.9'

services:
  onemedical-fe:
    image: onemedical-fe:demo5
    ports:
      - "4200:80"

  onemedical-be:
    image: stackoverfloww/onemedical-be:test
    env_file:
      - .env
    ports:
      - "8000:3000"

